请为算法或优化方法 `[算法名称]` 生成一份“总体代码框架文档”。

我的目标是查看这个算法在代码层面由哪些模块、类和函数组成，以及它们之间如何调用。

请只输出一棵完整的树形结构，不要写算法原理、公式、示例、优缺点或其他解释。

格式参考：

```text
Algorithm
│
├── 输入
│   └── ...
│
├── 输出
│   └── ...
│
├── 01_Module
│   │
│   ├── class ClassName
│   │   ├── attribute
│   │   └── attribute
│   │
│   └── function_name(input) -> output
│       ├── 输入：...
│       └── 输出：...
│
├── 02_Module
│   └── ...
│
└── Main_Solver
    └── run(problem, config) -> Result
        ├── 输入
        │   └── ...
        ├── 调用流程
        │   ├── function_1()
        │   ├── function_2()
        │   └── function_3()
        └── 输出
            └── Result
```

必须包括：

1. 算法的总体输入；
2. 算法的总体输出；
3. 核心数据类；
4. 配置类；
5. 结果类；
6. 主要功能模块；
7. 每个模块中的类；
8. 每个类的核心属性；
9. 每个模块中的函数；
10. 每个函数的参数；
11. 每个函数的返回值；
12. 每个函数下方标明“输入”和“输出”；
13. 主求解器或主循环；
14. 主求解器内部的函数调用顺序；
15. 模块之间的数据流。

写作要求：

- 使用中文；
- 使用 Markdown；
- 只输出一棵树形结构；
- 类名和函数名使用清晰的英文；
- 输入输出使用中文说明；
- 函数使用以下形式：

```text
function_name(parameter) -> ReturnType
├── 输入：...
└── 输出：...
```

- 类使用以下形式：

```text
class ClassName
├── attribute_1
├── attribute_2
└── method() -> ReturnType
```

- 所有类、函数、输入、输出和调用流程必须放在同一棵树中；
- 不要把函数单独拿出来逐节解释；
- 不要提供函数实现代码；
- 不要讲算法原理；
- 不要添加数学公式；
- 不要添加最小示例；
- 不要添加理解检查；
- 不要为了形式强行创建模块；
- 只保留实现该算法真正需要的模块、类和函数；
- 如果该算法通常依赖外部求解器，要把“用户需要实现的接口”和“求解器内部模块”分开；
- 最终内容应能直接复制到 `[算法名称]_Framework.md`，格式不能混乱。
