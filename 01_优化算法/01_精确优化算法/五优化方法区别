# 五种精确优化方法的区别与融合

## 1. 一句话区分

| 方法 | 主要解决什么 |
|---|---|
| LP | 连续数量怎么分 |
| IP | 整数方案怎么选 |
| MILP | 选不选，以及选后做多少 |
| QP | 怎么减少偏差、风险和波动 |
| CP | 怎么满足顺序、时间和复杂逻辑 |

---

## 2. 直观例子

假设工厂需要安排生产：

### LP：生产多少

```text
产品A生产多少吨？
产品B生产多少吨？
```

变量可以取小数：

```text
A = 2.5吨
B = 4.8吨
```

适合连续资源分配。

---

### IP：选哪个

```text
是否启动机器？
使用多少辆车？
选择哪些项目？
```

变量必须是整数：

```text
启动 = 0或1
车辆数 = 3
```

适合离散选择。

---

### MILP：选不选＋做多少

```text
是否启动机器？
启动后生产多少吨？
```

同时包含：

```text
y = 0或1：是否启动
x = 连续值：生产数量
```

适合离散选择和连续分配同时存在的问题。

---

### QP：偏差尽量小

```text
实际产量尽量接近计划产量
控制动作不要突然变化
投资风险尽量小
```

常用平方表示偏差：

```text
(实际值 - 目标值)²
```

偏差越大，惩罚越大。

---

### CP：顺序和冲突怎么安排

```text
工序A必须在工序B之前
一台机器不能同时加工两个任务
每道工序只能选择一台机器
```

适合：

- 排序；
- 调度；
- 时间安排；
- 资源互斥；
- 复杂逻辑。

---

## 3. 核心区别

| 方法 | 变量类型 | 主要特点 | 常见场景 |
|---|---|---|---|
| LP | 连续变量 | 线性目标和约束 | 生产量、运输量 |
| IP | 整数或0-1变量 | 处理离散选择 | 选址、车辆数量 |
| MILP | 整数＋连续变量 | 选择与数量同时优化 | 机器启停＋产量 |
| QP | 通常为连续变量 | 目标中包含平方项 | 风险、偏差、平滑 |
| CP | 布尔、整数、区间、序列 | 擅长逻辑和顺序 | 调度、排班 |

---

## 4. 它们之间的关系

```text
LP
└── 所有变量都可以取小数

IP
└── 变量必须取整数

MILP
└── 整数变量和连续变量同时存在

QP
└── 目标函数包含平方项

CP
└── 重点表达顺序、时间和逻辑关系
```

如果 MILP 加入二次目标，就形成：

```text
MILP + QP = MIQP
```

---

## 5. 可以怎样融合

### LP + IP/MILP

MILP 求解器内部经常先求 LP：

```text
MILP
  ↓ 暂时允许整数变量取小数
LP松弛
  ↓ 提供理论界
分支定界
  ↓
整数解
```

LP 帮助 MILP 排除不值得继续搜索的方案。

---

### MILP + QP

同时处理：

```text
是否启动机器
+
生产多少
+
产量偏差
```

形成 MIQP：

```text
整数选择
+
连续数量
+
二次偏差目标
```

---

### MILP + CP

```text
MILP
负责：
机器选择、生产数量、成本和容量

CP
负责：
工序顺序、开始时间和机器冲突
```

数据流：

```text
MILP确定资源配置
  ↓
CP安排具体执行顺序
  ↓
完整生产计划
```

---

### QP + 强化学习

```text
强化学习产生动作
  ↓
QP修正动作
  ↓
满足安全约束的动作
```

QP 可以让修正后的动作尽量接近强化学习原动作。

---

## 6. 怎么选择

```text
数量允许取小数？
→ LP

必须是整数或0/1？
→ IP

既有0/1选择，又有连续数量？
→ MILP

需要减少偏差、风险或波动？
→ QP

有大量顺序、时间和互斥关系？
→ CP

同时包含多种结构？
→ 分模块融合
```

---

## 7. 最后记忆

```text
LP：分多少

IP：选哪个

MILP：选哪个＋分多少

QP：偏差尽量小

CP：顺序和规则必须满足
```
