群智能算法的核心区别，就是“个体如何互相参考”。

| 算法 | 个体参考什么 | 更适合 |
|---|---|---|
| **PSO** | 自己历史最好＋群体最好 | 通用连续参数 |
| **GWO** | 当前前三名方案 | 结构简单的连续优化 |
| **FA** | 比自己更好的多个方案 | 存在多个较优区域 |
| **ABC** | 好方案附近重点搜索，停滞后重启 | 容易陷入局部最优的问题 |
| **ACO** | 所有个体积累的信息素 | 路径、排序、调度 |

一句话记忆：

```text
PSO：跟自己最好和全体最好
GWO：跟前三名
FA：跟比自己好的
ABC：好地方多搜，没进步就换地方
ACO：跟着历史信息素走
```

选择：

```text
连续参数 → 优先PSO、GWO
多峰连续问题 → FA、ABC
路径或排序问题 → ACO
```

融合情况
会，但一般只融合两个互补算法。

| 融合 | 作用 |
|---|---|
| **PSO + GWO** | PSO利用历史经验，GWO利用前三名引导 |
| **PSO + DE** | PSO负责移动，DE增加方案多样性 |
| **ABC + PSO** | ABC负责随机重启，PSO负责快速靠近好区域 |
| **FA + DE** | FA向好方案靠近，DE帮助跳出局部区域 |
| **ACO + PSO** | ACO处理路径顺序，PSO调整连续参数 |
| **ACO + 局部搜索** | ACO找路径，局部搜索进一步优化路径 |

常见结构：

```text
算法A
  ↓ 大范围寻找好区域
算法B
  ↓ 精细优化
最终方案
```

最值得融合的情况：

```text
离散决策＋连续参数
路径规划＋参数调整
全局搜索＋局部精调
```

如果两个算法功能基本相同，融合往往只是增加复杂度，不一定更好。
