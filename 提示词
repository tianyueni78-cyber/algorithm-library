提示词
请按照下面的方式介绍算法或优化方法：`[算法名称]`。
直观一点，别那么啰嗦。你太太太啰嗦了
我的主要目标不是学习复杂数学公式，而是理解：

1. 它在现实问题中到底起什么作用；
2. 它能替我完成什么，不能替我完成什么；
3. 哪些工作需要人完成，哪些工作由算法完成；
4. 如何把现实问题转换成该算法可以处理的输入；
5. 它的代码通常由哪些模块构成；
6. 每个模块的输入、输出和内部组成是什么；
7. 它适合与哪些其他算法融合。

请假设我是第一次接触这个算法。不要一开始就罗列术语、公式或抽象定义，也不要默认我已经理解“变量、约束、状态、动作、目标函数”等概念。

请严格按照以下固定顺序讲解。

# 1. 一句话说明它是什么

请用一句没有复杂术语的话说明：

- 它是什么；
- 它主要解决什么问题。

格式：

> `[算法名称]` 是一种……，主要用于……


# 3. 人和算法分别负责什么

使用下面的格式明确划分职责：

```text
现实问题
  ↓
人需要完成的工作
  ├── ...
  ├── ...
  └── ...
  ↓
算法需要完成的工作
  ├── ...
  ├── ...
  └── ...
  ↓
最终结果
```

必须明确回答：

- 人向算法提供什么；
- 算法接收到这些内容后做什么；
- 算法最后返回什么。


# 5. 从抽象现实问题中提取模型的方法

给出一套可以重复使用的提问框架。

至少包括：

```text
什么事情需要决定？
→ 决策变量、动作或输出

什么情况绝对不允许？
→ 约束、规则或可行性条件

什么结果算更好？
→ 目标函数、奖励或评价指标

哪些信息在决策前已经知道？
→ 输入数据、状态或参数

哪些信息会随着过程变化？
→ 动态状态或环境变化
```

请说明问题中的哪些自然语言关键词通常对应变量、约束、目标和输入数据。

# 6. 总体输入与输出

使用表格说明：

| 类型 | 内容 | 示例 |
|---|---|---|
| 输入 |  |  |
| 输出 |  |  |

同时区分：

- 原始业务输入；
- 建模后的算法输入；
- 算法内部数据；
- 最终业务输出。

# 7. 完整运行流程

用一个简单流程图表示：

```text
现实问题
  ↓
问题建模
  ↓
算法输入
  ↓
算法运行
  ↓
候选解或决策
  ↓
结果验证
  ↓
现实输出
```

请根据算法特点替换具体步骤。

# 8. 模块结构

列出该算法常见的软件模块。

格式：

```text
Algorithm
├── 01_Problem_Data
├── 02_...
├── 03_...
└── ...
```

不要为了形式强行拆出很多模块。只保留真正具有独立职责的模块。

# 9. 每个模块的输入、输出和构成

每个模块必须使用相同格式：

## 模块名称

### 作用

该模块解决什么具体问题。

### 输入

- 输入1
- 输入2

### 内部构成

```text
Module
├── ComponentA
├── ComponentB
└── ComponentC
```

### 处理过程

按照执行顺序解释该模块如何处理输入。

### 输出

- 输出1
- 输出2

### 是否需要用户自己实现

明确说明：

- 通常需要自己实现；
- 通常由算法库或求解器提供；
- 或者两者都可以。

# 10. 推荐代码结构

分别给出最小版本和完整版本。

最小版本：

```text
Algorithm_Project
├── ...
└── ...
```

完整版本：

```text
Algorithm_Project
├── ...
├── ...
└── ...
```

解释每个文件负责什么。

不要过度拆分，不要为单一功能创建没有必要的抽象层。

# 11. 用户需要实现什么

分成两部分：

## 通常需要自己实现

- ...

## 通常由算法库或求解器提供

- ...

如果不同软件库的边界不同，也请说明。

# 12. 它真正帮我解决什么

请直接回答：

- 它是在帮助我理解问题吗？
- 它是在帮助我建立模型吗？
- 它是在帮助我搜索答案吗？
- 它是在帮助我预测结果吗？
- 它是在帮助我做连续决策吗？
- 它是在帮助我验证解是否可行吗？

不要只回答“是”或“否”，需要说明边界。

# 13. 适用与不适用场景

使用表格：

| 比较适合 | 原因 |
|---|---|
|  |  |

| 不太适合 | 原因 |
|---|---|
|  |  |

# 14. 与相近算法比较

选择2～3个最容易混淆的方法进行比较。

使用表格：

| 对比项 | 当前算法 | 相近算法 |
|---|---|---|

重点比较：

- 输入形式；
- 输出形式；
- 是否需要模型；
- 如何搜索或学习；
- 是否保证可行；
- 是否追求最优；
- 适用问题。

# 15. 可以与其他算法融合的位置

画出简单的数据流：

```text
算法A
  ↓ 提供什么
当前算法
  ↓ 输出什么
算法B
```

然后说明可以融合的模块，例如：

- 初始解生成；
- 搜索策略选择；
- 参数自适应；
- 候选解修复；
- 目标评价；
- 动态决策；
- 局部优化。

不要只列出“算法A + 算法B”，必须解释：

- 为什么融合；
- 在哪个模块融合；
- 前一个算法输出什么；
- 后一个算法接收什么；
- 融合解决了什么问题。

# 16. 最小示例

给出一个非常小的示例，包括：

- 原始问题；
- 输入数据；
- 变量或状态；
- 规则或约束；
- 目标；
- 算法执行过程；
- 输出结果。

示例必须足够小，让人可以手工检查。

# 17. 一句话总结

使用下面的格式总结：

> 人负责……；`[算法名称]`负责……；最终输出……

# 18. 理解检查

最后只问我一个具体问题，检查我是否理解了：

- 什么由人定义；
- 什么由算法完成；
- 输入是什么；
- 输出是什么。

不要一次提出多个问题。

## 写作要求

- 使用中文；
- 使用固定编号标题，方便直接保存为 `.md`；
- 从现实作用开始，再介绍技术结构；
- 先讲直觉，再讲术语；
- 每出现一个术语，立即用现实语言解释；
- 必须明确“它能做什么”和“它不能做什么”；
- 多使用具体例子、表格和小型流程图；
- 数学公式只在确实有助于理解时出现；
- 不要堆砌定义；
- 不要只给模块名称，必须给出输入、输出和职责；
- 不要把软件库的功能误写成算法本身的能力；
- 如果 `[算法名称]` 存在多种含义，先说明本次采用哪一种含义。
